# NBM CLOUD v17.0 - Configuração de Ambiente
# Copie este arquivo para .env e configure as variáveis

# ===========================================
# CONFIGURAÇÕES BÁSICAS
# ===========================================

# Ambiente (development ou production)
NODE_ENV=production

# Porta do servidor (default: 3000)
PORT=3000

# Domínio do servidor (usado para callbacks de autenticação)
DOMAIN=nbmcloud.exemplo.com

# ===========================================
# BANCO DE DADOS (PostgreSQL)
# ===========================================

# URL de conexão completa
DATABASE_URL=postgresql://usuario:senha@localhost:5432/nbmcloud

# Ou configure individualmente:
# PGHOST=localhost
# PGPORT=5432
# PGUSER=nbmcloud
# PGPASSWORD=sua_senha_segura
# PGDATABASE=nbmcloud

# ===========================================
# SEGURANÇA
# ===========================================

# Chave secreta para sessões (gere com: openssl rand -base64 64)
SESSION_SECRET=sua_chave_secreta_muito_longa_e_segura_aqui

# ===========================================
# AUTENTICAÇÃO
# ===========================================

# Modo standalone (true = login local, false = Replit Auth)
# Em servidor local, use true para login com usuário/senha
STANDALONE_AUTH=true

# ===========================================
# ARMAZENAMENTO DE BACKUPS
# ===========================================

# Tipo de storage (local, gcs, s3)
STORAGE_TYPE=local

# Para storage local:
STORAGE_PATH=/opt/nbm-cloud/storage

# Para Google Cloud Storage:
# STORAGE_TYPE=gcs
# GCS_BUCKET=seu-bucket
# GCS_PROJECT_ID=seu-projeto
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Para AWS S3:
# STORAGE_TYPE=s3
# S3_BUCKET=seu-bucket
# S3_REGION=us-east-1
# AWS_ACCESS_KEY_ID=sua_access_key
# AWS_SECRET_ACCESS_KEY=sua_secret_key

# ===========================================
# CONFIGURAÇÕES OPCIONAIS
# ===========================================

# Limite de upload (em MB)
MAX_UPLOAD_SIZE=100

# Tempo de expiração da sessão (em segundos, default: 24h)
SESSION_MAX_AGE=86400

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# ===========================================
# EMAIL (para notificações)
# ===========================================

# SMTP_HOST=smtp.exemplo.com
# SMTP_PORT=587
# SMTP_USER=usuario@exemplo.com
# SMTP_PASSWORD=senha_smtp
# SMTP_FROM=NBM CLOUD <noreply@exemplo.com>
