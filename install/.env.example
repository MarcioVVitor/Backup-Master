# NBM - Network Backup Manager
# Arquivo de Configuracao de Ambiente
# Copie este arquivo para .env e configure os valores

# Conexao com o banco de dados PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/banco
DATABASE_URL=postgresql://nbm:sua_senha_aqui@localhost:5432/nbm

# Chave secreta para sessoes (gere uma string aleatoria longa)
# Exemplo: openssl rand -base64 48
SESSION_SECRET=sua_chave_secreta_muito_longa_aqui

# Porta do servidor web (padrao: 5000)
PORT=5000

# Ambiente de execucao (development ou production)
NODE_ENV=production

# (Opcional) URL do emissor OIDC para autenticacao
# ISSUER_URL=https://replit.com
